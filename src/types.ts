export type Configurations = {
  name: string;
  port?: number;
  dbPort?: number;
  dbName?: string;
  author?: string;
};

export type Profiler = {
  NAME: string;
  PORT?: number;
  AUTHOR?: string;
  DATABASE_PORT?: number;
  DATABASE_NAME?: string;
};
