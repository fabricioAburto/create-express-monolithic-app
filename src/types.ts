export type Configurations = {
  name: string;
  port?: number;
  dbPort?: number;
  author?: string;
};

export type Profiler = {
  NAME: string;
  PORT?: number;
  AUTHOR?: string;
  DATABASE_PORT?: number;
};
